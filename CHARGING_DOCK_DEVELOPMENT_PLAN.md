# 充电桩识别与自动对接功能开发计划书

> 文档版本：V1.1  
> 编写日期：2026-07-29  
> 适用项目：`Car_jetson`  
> 当前目标：在固定测试场地中，通过人工触发完成充电桩识别、车辆自动对准、低速靠近和停车。  
> 暂不包含：充电航点任务编排、充电桩供电控制、充电协议和充电完成后的任务恢复。

## 1. 项目背景

小车车尾已经安装充电接收面板，现场具有与之配套的充电桩发射面板。现有项目基于 ROS 2 Humble，已经接入 JD03 线控底盘、前后单线雷达、RoboSense 多线雷达、IMU、GNSS 和电池/BMS 状态反馈，并通过 `/cmd_vel -> /ecu` 控制底盘。

多地图导航消息已经定义 `type=3` 为充电桩航点，`navigation_manager.py` 中也保留了充电点处理分支，但目前没有充电桩识别和精确对接实现。

本阶段先将精确对接能力作为独立子系统开发，不依赖 Nav2、LIO-SAM、多地图导航或 MQTT 任务。完成独立验收后，再由 `NavigationManager` 在到达充电预对接航点时调用同一套对接接口。

## 2. 项目目标

### 2.1 本阶段目标

1. 将现有 USB 相机正式接入 ROS 2。
2. 完成相机内参标定以及相机到车体的外参标定。
3. 在不依赖 AprilTag、二维码等人工标记的条件下，识别真实充电桩和充电面板。
4. 检测充电面板关键点，通过相机模型计算充电桩相对车辆的位姿。
5. 根据纵向距离、横向偏差和航向偏差控制车辆低速倒车。
6. 在满足几何对准条件后停车并保持制动。
7. 对识别丢失、候选歧义、障碍物、控制超时和对接超时进行安全处理。
8. 通过人工命令启动、取消和重复执行对接测试。
9. 保留标准 Action 接口，便于后续接入充电航点逻辑。

### 2.2 本阶段不做的内容

1. 不修改路径规划，使车辆自动导航到充电区域。
2. 不处理 `type=3` 航点的自动触发逻辑。
3. 不控制充电桩通断电。
4. 不实现充电通信协议、充电时长控制和充满退出。
5. 不以 BMS `charge_status` 作为对接控制的必要输入。
6. 不追求识别任意品牌、任意外观充电桩的通用模型；第一版只覆盖当前充电桩型号和约定测试环境。

### 2.3 最终可交付能力

车辆被人工放置在允许的初始区域内后，操作人员发送一次对接命令，系统能够：

1. 找到指定充电桩。
2. 判断充电桩相对车尾的位置和方向。
3. 自动调整方向并倒车靠近。
4. 将车载接收面板与桩端发射面板对准。
5. 停车、制动并返回明确的成功或失败结果。

## 3. 当前项目基础与约束

### 3.1 已有可复用能力

| 能力 | 当前接口或位置 | 用途 |
| --- | --- | --- |
| JD03 底盘控制 | `/cmd_vel -> cmdvel_2_ecu -> /ecu` | 倒车、转向、停车 |
| 车辆状态 | `/vehicle_status` | 获取车速、转角、档位和驾驶模式 |
| BMS 状态 | `/battery_status` | 可选地观察充电状态 |
| 后向单线雷达 | `/laser/scan_back` | 对接区域障碍物和粗距离冗余 |
| 融合雷达 | `/laser/scan_merge_laser` | 外围安全监控 |
| USB 相机测试 | `real_ws/src/driver/camera/camera_test.py` | 已能通过 V4L2/OpenCV 读取图像 |
| TF 体系 | `base_link` 及各传感器坐标系 | 统一位姿变换 |

### 3.2 当前必须解决的问题

1. 相机尚未发布 ROS 图像和 `CameraInfo`。
2. 相机没有可靠的车尾固定安装和真实 TF。
3. 相机没有内参、畸变参数和标定文件。
4. 当前 `/cmd_vel` 小速度会被 `LOW_SPEED_THRESH=0.8` 强制抬升，不适合精细对接。
5. Nav2 和对接控制器如果同时发布速度，会形成双控制源。
6. 后向单线雷达的最小有效距离约为 `0.35 m`，不能覆盖最后贴近阶段。
7. 底盘为双阿克曼/四轮转向模型，不能像差速车一样原地旋转或横移。
8. 车载接收面板与桩端发射面板的垂直高度、俯仰和平面间距尚未形成标定数据。
9. 当前没有短距 ToF、机械接触开关或专用防撞触边接口。
10. 真实充电桩没有 AprilTag、二维码等人工视觉标记，需要采集数据训练专用识别和关键点模型。
11. 多个外观完全相同的充电桩无法仅凭图像确定物理编号，需要航点、地图位置或其他身份来源辅助关联。

### 3.3 核心原则

1. 测试时充电桩可以固定，但控制算法不得使用充电桩的地图绝对坐标。
2. 控制器只使用充电桩相对车辆的实时位姿，因此充电桩移动后算法仍然可用。
3. 视觉识别、位姿计算、运动控制和任务编排必须分层。
4. 任意关键输入失效时默认停车，而不是继续盲走。
5. 所有最终阈值必须可配置，不在代码中硬编码。
6. 正式运行不得依赖临时标记；临时测量标记只能用于研发阶段建立真值和评估误差。

## 4. 推荐技术方案

### 4.1 无标记充电桩识别

正式运行采用“目标检测 + 关键点检测 + 几何位姿估计”，不要求在充电桩上增加 AprilTag、二维码或其他人工视觉标记。

第一版模型只识别当前照片和现场中的充电桩型号，建议采用能够同时输出边界框和关键点的姿态检测模型。模型至少输出：

1. 充电桩或桩端充电组件的边界框。
2. 桩端充电面板四个角点。
3. 充电面板几何中心。
4. 外壳上至少两个稳定、非对称的结构关键点。
5. 目标置信度和各关键点可见性。

只检测黑色矩形或中央“+”号不够可靠。真实数据集必须加入车载接收面板、黑色箱体、门板和类似矩形物体作为负样本，避免模型把非充电桩物体误认为目标。

### 4.2 数据集与关键点定义

计划采集 `1000~3000` 张有效图像作为第一版数据集，数量根据基线模型效果继续增加。数据应覆盖：

1. 距离 `0.3~3.0 m`。
2. 横向偏差至少 `±0.5 m`。
3. 航向偏差至少 `±25 deg`。
4. 明亮、普通、昏暗、逆光、阴影和面板反光。
5. 完整可见、局部遮挡、运动模糊和不同背景。
6. 单充电桩、多候选物体以及没有充电桩的负样本。
7. 不同相机曝光、车辆负载和拍摄日期。

建议关键点顺序固定为：

```text
panel_top_left
panel_top_right
panel_bottom_right
panel_bottom_left
panel_center
housing_reference_left
housing_reference_right
```

充电面板接近方形且外观较对称，仅使用四角容易出现方向翻转。额外的外壳非对称关键点用于确定上下、左右和面板法向。若现有外壳没有稳定非对称结构，应选择固定螺栓、支架边缘或背板角点，不能使用可能移动的电缆作为定位点。

数据集划分必须按拍摄日期、场景或完整视频序列进行，禁止将同一段连续视频的相邻帧随机分散到训练集、验证集和测试集，否则离线指标会虚高。

### 4.3 位姿估计

测量并记录各关键点在 `dock_charge_link` 下的真实三维坐标。识别节点根据相机内参、畸变参数和二维关键点，通过 PnP 计算桩端充电面板相对相机的位姿。

实现要求：

1. 对平面目标使用适合平面点集的 PnP 方法，并处理可能的双解问题。
2. 根据关键点置信度筛除不可见或误检点。
3. 使用重投影误差、距离范围、面板尺寸和法向方向进行结果门控。
4. 使用 RANSAC 或多假设评估排除离群关键点。
5. 将相机位姿转换到 `base_link`，得到对接控制需要的平面误差。
6. 使用后向雷达对视觉距离进行粗校验，但不把雷达作为最后 `0.35 m` 内的唯一距离来源。

临时 AprilTag、测量尺或全站仪可以在研发阶段用于建立位姿真值和评估 PnP 误差，但不得成为正式运行输入。

### 4.4 多充电桩候选与身份关联

识别模型负责判断“这是充电桩”，但无法凭空判断多个外观完全相同设备的物理编号。因此身份选择分两种模式。

单桩独立测试模式：

1. 测试区域内只允许存在一个有效充电桩。
2. 在约定搜索区域内选择置信度最高且通过几何检查的候选。
3. 如果同时存在多个有效候选，则报告歧义并停车，不进行随机选择。

正式多桩模式：

1. 使用 `waypoint.id -> dock_id -> 地图近似位姿` 建立配置关系。
2. 到达充电航点后，根据目标桩地图位置预测其相对方位和距离。
3. 检测视野内全部充电桩，并将各候选位姿转换到地图坐标。
4. 在位置、方向和距离门限内选择与目标 `dock_id` 最匹配的候选。
5. 一旦锁定候选，后续使用时序跟踪保持实例一致；候选切换必须重新进入确认状态。
6. 无匹配或多个候选同样合理时返回“身份歧义”，禁止开始对接。

如果多个充电桩外观完全相同、相互靠近、位置还会变化，并且系统没有地图约束、可见编号或无线身份，那么单目视觉无法可靠确定物理编号。此类现场必须额外提供固定泊位关系、OCR 编号、通信 ID 或其他身份来源，这属于可观测性约束，不应由控制算法猜测。

### 4.5 模型部署

训练在具备 GPU 的开发环境完成，部署侧输出 ONNX，并在 Jetson 上使用 TensorRT FP16 作为首选推理后端。部署节点必须：

1. 固定输入分辨率和预处理方式。
2. 发布模型版本、推理耗时和输入时间戳。
3. 保留调试图像开关，生产模式默认关闭高带宽调试输出。
4. 模型加载失败、推理超时或输出异常时报告故障并禁止运动。
5. 记录离线训练模型与车端 TensorRT Engine 的对应关系。

### 4.6 系统数据流

```text
后向 USB 相机
    -> /rear_camera/image_raw + /rear_camera/camera_info
    -> 无标记充电桩检测与关键点模型
    -> /charging_dock/detections + /charging_dock/keypoints
    -> 候选筛选与 dock_id 关联
    -> PnP 位姿估计
    -> /charging_dock/dock_pose
    -> 位姿变换与滤波
    -> 纵向/横向/航向误差
    -> Dock Action Server 状态机
    -> /cmd_vel_dock
    -> 速度指令仲裁器
    -> /cmd_vel_selected
    -> cmdvel_2_ecu 对接低速模式
    -> /ecu
    -> JD03 底盘
```

辅助输入：

```text
/vehicle_status       -> 实际车速、档位和转向反馈
/laser/scan_back      -> 后向障碍物和视觉距离粗校验
/battery_status       -> 对接完成后的可选充电状态观察
/tf                   -> camera、receiver、base_link、map 坐标变换
目标 dock_id/地图先验  -> 多充电桩候选关联；单桩测试阶段可为空
```

### 4.7 坐标系设计

至少定义以下坐标系：

| 坐标系 | 定义 |
| --- | --- |
| `base_link` | 车辆控制参考中心，遵循项目现有定义 |
| `receiver_link` | 车载充电接收面板的几何中心和法向 |
| `rear_camera_link` | 后向相机机体坐标系 |
| `rear_camera_optical_frame` | ROS 光学坐标系 |
| `dock_charge_link` | 由关键点和 PnP 估计的桩端充电面板中心和法向 |
| `dock_target_link` | 车辆完成对接时 `receiver_link` 应到达的目标位姿 |

位姿计算链：

```text
base_link -> rear_camera_link -> rear_camera_optical_frame
rear_camera_optical_frame -> dock_charge_link -> dock_target_link
base_link -> receiver_link
```

控制器比较 `receiver_link` 与 `dock_target_link`，输出：

1. 纵向剩余距离 `e_longitudinal`。
2. 横向中心偏差 `e_lateral`。
3. 平面航向误差 `e_yaw`。

## 5. 软件包和文件规划

建议在 `real_ws/src` 中增加两个独立 ROS 2 包：

```text
real_ws/src/
├── charging_dock_msgs/
│   ├── action/
│   │   └── Dock.action
│   ├── msg/
│   │   ├── Keypoint2D.msg
│   │   ├── DockDetection.msg
│   │   ├── DockDetectionArray.msg
│   │   └── DockState.msg
│   ├── CMakeLists.txt
│   └── package.xml
└── charging_dock/
    ├── charging_dock/
    │   ├── dock_detector.py
    │   ├── dock_keypoint_model.py
    │   ├── dock_candidate_associator.py
    │   ├── dock_pose_estimator.py
    │   ├── dock_pose_filter.py
    │   ├── dock_controller.py
    │   ├── command_mux.py
    │   └── safety_monitor.py
    ├── config/
    │   ├── camera_info.yaml
    │   ├── dock_geometry.yaml
    │   ├── dock_registry.yaml
    │   ├── model.yaml
    │   ├── controller.yaml
    │   └── safety.yaml
    ├── models/
    │   ├── README.md
    │   └── model_metadata.yaml
    ├── tools/
    │   ├── extract_dataset_frames.py
    │   ├── validate_annotations.py
    │   └── evaluate_pose.py
    ├── launch/
    │   ├── rear_camera.launch.py
    │   └── docking_test.launch.py
    ├── test/
    │   ├── test_candidate_association.py
    │   ├── test_pnp_pose.py
    │   ├── test_pose_transform.py
    │   ├── test_pose_filter.py
    │   ├── test_controller.py
    │   └── test_state_machine.py
    ├── package.xml
    └── setup.py
```

如果采用系统提供的 `v4l2_camera` 或 `usb_cam` 作为图像发布器，则 `charging_dock` 不再自行实现底层相机采集，只保留相机启动和参数配置。

## 6. ROS 接口设计

### 6.1 Dock Action

第一版建议定义以下 Action 概念字段：

```text
# Goal
int32 dock_id
float32 timeout_sec
bool dry_run
---
# Result
bool success
uint8 error_code
string message
float32 final_lateral_error
float32 final_longitudinal_error
float32 final_yaw_error
---
# Feedback
uint8 state
float32 lateral_error
float32 longitudinal_error
float32 yaw_error
float32 detection_quality
```

`dry_run=true` 时只运行识别、位姿计算和状态机，不向底盘发送非零速度，用于静态联调。

`dock_id` 约定：

1. 单桩独立测试阶段使用 `dock_id=0`，表示只接受视野中的唯一有效候选。
2. 正式多桩阶段使用大于零的固定 ID，并从 `dock_registry.yaml` 读取地图先验和关联参数。
3. `dock_id=0` 时如果同时出现多个有效候选，Action 必须返回身份歧义，不能选择最高置信度后直接运动。

### 6.2 计划话题

| 话题 | 类型 | 方向 | 说明 |
| --- | --- | --- | --- |
| `/rear_camera/image_raw` | `sensor_msgs/Image` | 输入 | 后向相机原始图像 |
| `/rear_camera/camera_info` | `sensor_msgs/CameraInfo` | 输入 | 相机内参和畸变参数 |
| `/charging_dock/detections` | 自定义检测数组 | 中间输出 | 全部充电桩候选、边界框和置信度 |
| `/charging_dock/keypoints` | 自定义关键点数组 | 中间输出 | 候选面板关键点、置信度和可见性 |
| `/charging_dock/dock_pose` | `geometry_msgs/PoseStamped` | 中间输出 | 选中桩端充电面板相对相机的位姿 |
| `/charging_dock/target_pose` | `geometry_msgs/PoseStamped` | 中间输出 | 最终目标相对车辆的位姿 |
| `/charging_dock/state` | 自定义或标准消息 | 输出 | 当前对接状态和故障码 |
| `/cmd_vel_dock` | `geometry_msgs/Twist` | 输出 | 对接控制速度 |
| `/cmd_vel_nav` | `geometry_msgs/Twist` | 输入 | 预留的 Nav2 速度通道 |
| `/cmd_vel_selected` | `geometry_msgs/Twist` | 输出 | 仲裁后唯一底盘速度 |

### 6.3 错误码建议

| 错误码 | 含义 |
| --- | --- |
| `0` | 成功 |
| `1` | 未找到指定充电桩 |
| `2` | 识别中途丢失 |
| `3` | 对接超时 |
| `4` | 后方存在非预期障碍物 |
| `5` | 车辆状态不可用或超时 |
| `6` | 控制输出超时 |
| `7` | 初始位置超出允许范围 |
| `8` | 多次重试后仍无法对准 |
| `9` | 操作人员取消或急停 |
| `10` | 检测到多个候选且无法确定目标桩 |
| `11` | 模型未加载、推理超时或输出无效 |
| `12` | PnP 重投影误差或几何检查不通过 |

## 7. 对接状态机设计

```text
IDLE
  -> ACQUIRE
  -> VALIDATE_INITIAL_POSE
  -> ALIGN
  -> APPROACH
  -> FINAL_APPROACH
  -> VERIFY
  -> DOCKED
```

任何运动状态均可进入：

```text
STOPPING -> RETRY 或 FAILED
```

### 7.1 状态说明

#### IDLE

1. 输出零速度并保持制动。
2. 等待 Dock Action 请求。
3. 检查相机、TF、车辆状态和后雷达新鲜度。

#### ACQUIRE

1. 检测视野内全部充电桩候选和面板关键点。
2. 单桩测试模式选择唯一有效候选；多桩模式使用 `dock_id` 和地图先验完成关联。
3. 要求同一候选连续若干帧识别、关键点和 PnP 位姿有效。
4. 检查类别置信度、关键点可见性、重投影误差、位姿跳变和几何合理性。
5. 候选不唯一、身份不明确或超时未找到时返回失败，不允许盲目倒车搜索。

#### VALIDATE_INITIAL_POSE

检查初始状态是否位于控制器允许区域内，例如：

1. 纵向距离在 `0.5~2.0 m` 范围内。
2. 横向偏差不超过第一阶段测试范围。
3. 航向误差不超过第一阶段测试范围。
4. 车辆当前速度接近零。
5. 自动驾驶控制已使能。

#### ALIGN

1. 以较低倒车速度沿可行圆弧修正横向和航向误差。
2. 控制命令必须符合双阿克曼运动约束。
3. 不尝试横移或原地旋转。
4. 如误差持续增大，立即停车并判定控制符号或初始条件异常。

#### APPROACH

1. 随距离缩短逐步降低速度。
2. 持续修正小范围横向和航向误差。
3. 后雷达用于检测非预期障碍物，但不能作为最后距离的唯一依据。

#### FINAL_APPROACH

1. 最后约 `0.20~0.30 m` 原则上保持近似直线倒车。
2. 限制最大转角和最大速度。
3. 若横向或航向误差超限，停车并前移退出一小段距离后重试。
4. 不允许在充电桩前进行大转角强行纠偏。

#### VERIFY

1. 连续多帧检查最终三项误差。
2. 发布零速度并确认 `/vehicle_status` 中实际车速归零。
3. 保持制动一定时间。
4. 可选观察 `/battery_status.charge_status`，但其结果不影响本阶段几何对接验收。

#### DOCKED

1. 返回成功结果和最终误差。
2. 保持零速度和制动。
3. 未收到退出命令前不释放运动控制权。

#### RETRY

1. 停车确认后，低速前移至安全重试距离。
2. 重新进入 `ACQUIRE`。
3. 重试次数达到上限后进入 `FAILED`。

## 8. 控制策略

### 8.1 控制输入

控制器使用滤波后的：

```text
e_longitudinal
e_lateral
e_yaw
```

控制循环建议从 `20 Hz` 开始，实际频率不得高于相机有效输出和位姿更新时间。

### 8.2 控制输出

输出常规 ROS `Twist`：

1. `linear.x < 0` 表示倒车。
2. `angular.z` 根据项目现有 `cmdvel_2_ecu` 符号约定生成。
3. 实车测试前必须验证正负转向在倒车状态下的实际方向。

### 8.3 速度分段初值

以下仅作为联调初值，必须在底盘低速测试后修改：

| 剩余距离 | 候选倒车速度 |
| --- | --- |
| 大于 `0.60 m` | `0.10~0.15 m/s` |
| `0.20~0.60 m` | `0.05~0.08 m/s` |
| 小于 `0.20 m` | `0.02~0.04 m/s` 或脉冲移动 |

如果 ECU 无法稳定执行连续微速，则采用：

```text
短时间移动 -> 刹车 -> 等待车体稳定 -> 重新测量 -> 再移动
```

### 8.4 位姿滤波

第一版建议组合使用：

1. 位姿有效范围检查。
2. 连续帧平移和角度跳变阈值。
3. 短窗口中值滤波。
4. 指数低通或 One Euro Filter。
5. 时间戳和数据新鲜度检查。

滤波不得掩盖检测丢失、候选切换或 PnP 失效。超过规定时间未收到同一候选的有效位姿，必须立即进入停车流程。

## 9. 速度指令仲裁与底盘改造

### 9.1 目标结构

```text
Nav2       -> /cmd_vel_nav  ----\
                                -> command_mux -> /cmd_vel_selected -> cmdvel_2_ecu
Dock 控制器 -> /cmd_vel_dock ----/
```

仲裁要求：

1. 同一时刻只允许一个控制源。
2. 对接控制源在 Dock Action 有效期间优先。
3. 任意控制源超时后立即输出零速度。
4. 控制权切换时先输出零速度并确认车辆停车。
5. 对接控制器退出时必须连续发布若干次零速度。

### 9.2 `cmdvel_2_ecu` 改造要求

1. 增加正常导航和精确对接两套速度限制参数。
2. 对接模式不使用当前 `LOW_SPEED_THRESH=0.8` 的强制下限。
3. 对接模式限制最大倒车速度、最大转角和加速度变化。
4. 对接模式下输入超时必须制动。
5. 明确 `/cmd_vel` 的单位与 ECU 实际目标速度单位。
6. 使用实车数据验证倒车时 `angular.z -> steer` 的符号和比例。

不建议让对接控制器直接发布 `/ecu`，否则会绕过现有转换逻辑并产生多个 ECU 控制源。

## 10. 分阶段开发计划

### 阶段 0：硬件基线和安全准备

任务：

- [ ] 确认 USB 相机实际型号、设备节点、稳定序列号和最大稳定帧率。
- [ ] 确认相机能够固定安装在车尾并持续看到充电桩。
- [ ] 测量车载和桩端充电面板的尺寸、中心、高度和允许间距。
- [ ] 确认人工遥控急停可用。
- [ ] 清理测试区域并设置车辆最大活动边界。
- [ ] 实测底盘最小稳定倒车速度和制动距离。
- [ ] 确认 `/vehicle_status` 反馈频率和车速零点。
- [ ] 确认 `/laser/scan_back` 数据方向和有效范围。
- [ ] 确认当前单桩测试区域内不会同时出现第二个有效充电桩。
- [ ] 获取充电桩外形、面板和稳定结构点的实测尺寸。

完成标志：形成硬件测量记录和低速测试记录；任何自动运动前急停和零速度保护已验证。

### 阶段 1：相机 ROS 化与内参标定

任务：

- [ ] 使用稳定 `/dev/serial/by-id`、udev 名称或 VID/PID 锁定相机。
- [ ] 选择并固定运行分辨率和帧率，建议先使用 `1280x720@30fps`。
- [ ] 发布 `/rear_camera/image_raw` 和 `/rear_camera/camera_info`。
- [ ] 使用棋盘格或 ChArUco 完成相机内参标定。
- [ ] 保存标定日期、相机序列号、分辨率和标定板尺寸。
- [ ] 检查去畸变图像和重投影误差。
- [ ] 完成持续运行和断连恢复测试。

完成标志：相机能够稳定发布至少 `20 Hz` 图像；运行分辨率与标定分辨率一致；重投影误差目标小于 `0.5 px`。

### 阶段 2：无标记充电桩数据集

任务：

- [ ] 固定模型类别定义和关键点顺序。
- [ ] 测量所有关键点在 `dock_charge_link` 下的三维坐标。
- [ ] 编写采集清单并覆盖距离、横向、航向、照明、遮挡和反光条件。
- [ ] 采集当前充电桩 `1000~3000` 张候选有效图像。
- [ ] 采集车载接收面板、黑色箱体、门板和类似矩形物体负样本。
- [ ] 标注充电桩边界框、面板关键点、关键点可见性和场景属性。
- [ ] 编写标注检查脚本，检查关键点顺序、越界、漏标和重复帧。
- [ ] 按日期、完整视频序列或场景划分训练、验证和测试集。
- [ ] 对数据集版本、采集设备、标注规范和划分方式建立记录。

完成标志：形成版本固定、无跨序列泄漏、能够复现的数据集，并通过人工抽检和自动标注检查。

### 阶段 3：模型训练和 Jetson 部署

任务：

- [ ] 训练第一版充电桩检测和关键点基线模型。
- [ ] 分别统计目标检测、关键点精度以及负样本误检情况。
- [ ] 分场景评估距离、角度、照明、遮挡和反光条件。
- [ ] 检查相似黑色物体和车载接收面板上的误检。
- [ ] 对困难样本进行补采、补标和第二轮训练。
- [ ] 导出 ONNX 并验证与训练框架输出一致。
- [ ] 在目标 Jetson 上生成 TensorRT FP16 Engine。
- [ ] 验证车端预处理、后处理、关键点顺序和置信度一致。
- [ ] 测量 Jetson 推理帧率、端到端延迟、GPU/内存占用和温度稳定性。
- [ ] 发布检测结果、关键点、模型版本和可选调试图像。

完成标志：模型在独立测试集和目标 Jetson 上达到第 12 节指标，连续运行无内存增长和明显热降频。

### 阶段 4：候选关联、PnP 位姿和 Dry Run

任务：

- [ ] 标定或测量 `base_link -> rear_camera_link`。
- [ ] 标定或测量 `base_link -> receiver_link`。
- [ ] 配置 `dock_charge_link -> dock_target_link` 目标间距。
- [ ] 根据二维关键点、关键点三维坐标和相机内参实现 PnP。
- [ ] 实现关键点置信度、重投影误差、距离、尺寸和法向门控。
- [ ] 实现单桩测试模式的唯一候选选择。
- [ ] 实现多候选歧义检测，歧义时返回错误而不是随机选择。
- [ ] 为后续多桩模式定义 `dock_registry.yaml` 和 `waypoint.id -> dock_id` 关系。
- [ ] 实现完整 TF 变换链和误差计算。
- [ ] 在 RViz 中显示候选框、关键点、充电中心、目标位姿和误差方向。
- [ ] 实现 `dry_run=true`，只打印控制量但不动车。
- [ ] 人工改变车体位置，确认建议控制方向始终正确。
- [ ] 使用临时测量基准评估视觉 PnP 位姿误差；正式运行不依赖临时标记。
- [ ] 为候选关联、PnP、位姿变换、角度归一化和误差符号增加单元测试。

完成标志：不启动车辆即可完整执行 Dock Action，输出的控制方向与人工判断一致。

### 阶段 5：底盘低速控制与指令仲裁

任务：

- [ ] 增加 `/cmd_vel_nav` 和 `/cmd_vel_dock` 控制通道。
- [ ] 实现带优先级、超时和零速度切换的命令仲裁器。
- [ ] 为 `cmdvel_2_ecu` 增加对接低速模式。
- [ ] 验证前进、倒车、左转、右转和刹车指令。
- [ ] 测量不同低速指令的实际车速和停车距离。
- [ ] 验证控制节点退出或崩溃时车辆自动停车。
- [ ] 确定连续微速或脉冲移动方案。

完成标志：人工发送 `/cmd_vel_dock` 时车辆可以受限低速运动；停止发布后在规定时间内制动；Nav2 通道不能同时获得控制权。

### 阶段 6：闭环对准和靠近

按风险从低到高逐步开放：

1. 只允许直线倒车和距离停车。
2. 加入小航向误差修正。
3. 加入小横向偏差修正。
4. 加入分段降速。
5. 加入最终直线靠近。
6. 加入退出重试和失败处理。

初始测试范围：

| 参数 | 第一轮范围 |
| --- | --- |
| 初始距离 | `1.0~1.5 m` |
| 横向偏差 | `±0.10 m` |
| 航向偏差 | `±5 deg` |
| 最大倒车速度 | 由阶段 4 实测决定，且不超过候选上限 |
| 重试次数 | `0~1` 次 |

稳定后逐步扩展到：

| 参数 | 扩展目标 |
| --- | --- |
| 初始距离 | `0.5~2.0 m` |
| 横向偏差 | `±0.30 m` |
| 航向偏差 | `±15 deg` |
| 重试次数 | 最多 `2~3` 次 |

完成标志：车辆可以从第一轮范围内自动完成对准和停车，且任意识别丢失或取消操作都会停车。

### 阶段 7：安全和异常测试

任务：

- [ ] 对接过程中遮挡充电面板或关键结构点。
- [ ] 在视野内放置相似黑色物体，验证不会误选目标。
- [ ] 同时放置两个有效候选，验证单桩模式报告身份歧义。
- [ ] 模拟模型加载失败、推理超时和无效关键点输出。
- [ ] 模拟 PnP 重投影误差超限或法向翻转。
- [ ] 停止相机节点或拔掉相机。
- [ ] 停止 `/vehicle_status`。
- [ ] 在后方放置非充电桩障碍物。
- [ ] 人工取消 Dock Action。
- [ ] 杀死对接控制节点。
- [ ] 模拟 TF 缺失或时间戳过期。
- [ ] 让初始位姿超出允许范围。
- [ ] 模拟车辆无运动、打滑或误差持续增大。
- [ ] 验证最大运动距离、最大执行时间和最大重试次数。

完成标志：所有异常均返回明确错误码并停车，不出现失控、继续盲走或控制权残留。

### 阶段 8：重复性验收

进行至少 50 次独立对接测试，覆盖：

1. 不同初始距离。
2. 左右横向偏差。
3. 正负航向偏差。
4. 明亮、普通和较暗照明。
5. 局部遮挡和背景变化。
6. 不同电池电量和车辆负载。
7. 有相似负样本但没有充电桩的场景。
8. 单个充电桩与多个候选歧义场景。

每次记录：

1. 是否成功。
2. 总执行时间。
3. 重试次数。
4. 最终三项误差。
5. 最大速度和制动距离。
6. 失败阶段和错误码。
7. 检测置信度、关键点质量和 PnP 重投影误差。
8. 推理延迟和感知数据年龄。

完成标志：满足第 12 节验收标准，并形成测试报告和 rosbag 数据集。

## 11. 测试与数据记录

### 11.1 建议录制话题

```bash
ros2 bag record \
  /rear_camera/image_raw \
  /rear_camera/camera_info \
  /charging_dock/detections \
  /charging_dock/keypoints \
  /charging_dock/dock_pose \
  /charging_dock/target_pose \
  /charging_dock/state \
  /cmd_vel_dock \
  /cmd_vel_selected \
  /vehicle_status \
  /battery_status \
  /laser/scan_back \
  /tf \
  /tf_static
```

### 11.2 测试分层

1. 单元测试：坐标变换、角度归一化、滤波器、状态机和控制限幅。
2. 模型离线评估：使用独立测试集验证检测、关键点和相似物体误检。
3. rosbag 离线回放：验证候选关联、PnP、滤波和控制计算，不连接底盘。
4. Dry Run：连接实车传感器但禁止非零速度。
5. 低速开环：人工发送受限速度，验证底盘响应。
6. 闭环分段：直线、航向、横向、最终靠近逐步开放。
7. 故障注入：拔设备、杀节点、遮挡、候选歧义和放置障碍物。
8. 重复性验收：固定测试矩阵并统计成功率。

### 11.3 必须保存的信息

1. 相机设备序列号和 USB 路径。
2. 相机标定原图、标定板参数和 `camera_info.yaml`。
3. 数据集原图、标注规范、关键点定义、划分清单和数据集版本。
4. 充电桩各关键点在 `dock_charge_link` 下的实测三维坐标。
5. 训练配置、训练日志、权重、离线指标、ONNX 和 TensorRT 元数据。
6. 所有静态 TF 的测量方法和数值。
7. 充电面板允许的实际对准误差。
8. 底盘最小稳定速度和刹停距离。
9. 每次测试使用的 Git 提交号和参数文件。
10. 成功与失败 rosbag。

## 12. 验收标准

以下为第一版工程目标，最终应以充电硬件厂家提供的允许偏差为准。

### 12.1 相机和识别

- [ ] 相机持续输出不低于 `20 Hz`。
- [ ] 运行分辨率与标定分辨率一致。
- [ ] 标定重投影误差目标小于 `0.5 px`。
- [ ] 独立测试集不包含训练集相邻视频帧或同序列泄漏。
- [ ] 当前充电桩检测在验收场景中的 Precision 和 Recall 目标均不低于 `98%`。
- [ ] 关键点指标达到训练阶段确定的阈值，并且角点顺序无系统性翻转。
- [ ] 在 `0.3~2.0 m` 对接工作范围内稳定识别充电桩和充电面板关键点。
- [ ] 相似黑色物体、车载接收面板和空场景不触发可执行的对接目标。
- [ ] 多候选无法唯一关联时明确报告身份歧义。
- [ ] Jetson 端有效感知输出不低于 `15 Hz`，感知链路 95 分位延迟目标不超过 `150 ms`。
- [ ] `1.0 m` 内视觉位姿横向 RMSE 目标不超过 `10 mm`，航向 RMSE 目标不超过 `1 deg`。
- [ ] 识别丢失能够在 `200 ms` 量级触发停车流程。
- [ ] 静止时位姿不出现会导致车辆误动作的跳变。

### 12.2 最终对准

- [ ] 横向误差目标不超过 `±20 mm`。
- [ ] 航向误差目标不超过 `±2 deg`。
- [ ] 面板间距进入实测目标值的 `±10 mm` 窗口。
- [ ] 车辆实际速度确认归零并保持制动。
- [ ] 最终阈值连续满足规定帧数后才返回成功。

### 12.3 安全和可靠性

- [ ] 50 次测试总体成功率不低于 `95%`。
- [ ] 无碰撞、无越界和无失控运动。
- [ ] 取消、识别丢失、输入超时和节点异常均能停车。
- [ ] 任意时刻只有一个速度控制源。
- [ ] 达到最大时间、距离或重试次数后明确失败。
- [ ] 失败后车辆保持静止，不自动恢复运动。

## 13. 主要风险及应对

| 风险 | 影响 | 应对措施 |
| --- | --- | --- |
| 训练数据场景单一 | 更换照明或背景后漏检 | 按场景采集数据；按视频序列划分；持续收集失败样本再训练 |
| 充电面板接近对称 | 关键点翻转、PnP 法向错误 | 增加稳定非对称外壳关键点；检查法向、尺寸和重投影误差 |
| 近距离关键点离开相机视野 | 最后阶段丢失定位 | 使用宽视场后向相机；优化安装位置；保证至少一组定位结构始终可见 |
| 相机支架振动或移动 | 外参失效 | 刚性安装、加定位销、每次测试前检查 TF 基准 |
| 黑色面板反光或照明变化 | 图像质量和关键点精度下降 | 将反光和暗光纳入数据集；限制曝光变化；增加均匀补光 |
| 相似黑色物体误检 | 对错误目标执行倒车 | 强化负样本；几何门控；连续帧确认；雷达粗校验 |
| 多个外观相同充电桩 | 无法确认目标物理编号 | 使用 waypoint/dock 地图关联；歧义即停；现场必要时增加编号或通信 ID |
| Jetson 推理延迟或热降频 | 控制使用过期位姿 | TensorRT FP16、限制模型规模、监控数据年龄和温度、超时即停 |
| 当前最低速度过高 | 最终位置超调 | 对接低速模式；分段降速；必要时脉冲移动 |
| 双阿克曼不能横移 | 大横向偏差无法近距离修正 | 在较远距离完成纠偏；最终误差超限时前移重试 |
| 后雷达存在近距离盲区 | 最后阶段无法独立防撞 | 极低速、视觉距离、运动上限、人工急停；量产阶段考虑增加短距传感器 |
| 相机或 TF 数据延迟 | 控制振荡或误动作 | 检查时间戳、限制数据年龄、控制限幅、丢帧即停 |
| Nav2 与 Dock 同时发速度 | 指令冲突 | 强制命令仲裁和控制权状态机 |
| 垂直高度或俯仰不一致 | 平面已对准但无法充电 | 通过机械结构调节，软件只负责平面 3 自由度 |
| 充电硬件容差未知 | 验收阈值不可靠 | 获取厂家规格或通过静态通电试验测量有效窗口 |

## 14. 后续接入充电航点的预留方案

本阶段验收后，再进行以下集成：

1. 将 `type=3` 航点的 `x/y/yaw` 定义为充电桩预对接车体位姿，而不是充电桩实体中心。
2. 使用 `Waypoint.id` 或新增字段建立 `waypoint -> dock_id` 关系。
3. 为每个固定充电桩在 `dock_registry.yaml` 中配置地图近似位姿、允许关联半径和外观模型版本。
4. Nav2 只负责到达预对接位。
5. `NavigationManager` 在 Nav2 成功后将目标 `dock_id` 传给 `Dock` Action。
6. 感知节点检测所有候选，通过地图位置、方向和距离选择目标桩；不能唯一选择时任务失败。
7. 充电点即使是最终航点，也必须先执行 Dock Action，再结束导航任务。
8. 当前 `on_goal_reached()` 需要先按航点类型分发，再判断是否完成整条路线。
9. 对接成功后再根据产品需求决定发布 `idle`、`charging` 或保持任务状态。
10. 可选使用 `/battery_status.charge_status` 验证实际开始充电。

这一集成阶段不得修改已经通过独立验收的视觉和控制核心，只增加 Action Client、状态转换和任务结果上报。

## 15. 开发完成定义（Definition of Done）

满足以下条件后，本阶段才视为完成：

- [ ] 相机 ROS 驱动、标定文件和 TF 已纳入版本管理。
- [ ] 无标记充电桩数据集、标注规范、划分清单和版本信息已归档。
- [ ] 充电桩检测与关键点模型通过独立测试集和 Jetson 部署验收。
- [ ] ONNX、TensorRT Engine 和模型元数据之间可以追溯。
- [ ] 充电面板关键点三维坐标和对接几何偏移有书面记录。
- [ ] 单桩唯一候选和多候选歧义策略已经验证。
- [ ] Dock Action 支持启动、反馈、取消、成功和明确失败。
- [ ] Dry Run、低速开环、闭环和故障注入测试全部完成。
- [ ] 命令仲裁和控制超时已经实车验证。
- [ ] 对接控制器不依赖地图、Nav2 或充电航点。
- [ ] 至少 50 次重复测试达到验收成功率。
- [ ] 成功与失败 rosbag、测试报告和最终参数均已归档。
- [ ] 操作人员具有明确的启动、取消、急停和故障恢复说明。
- [ ] 后续 NavigationManager 集成接口已经固定，不需要重写核心算法。
