# 充电桩识别与自动对接功能开发计划书（方案 A：AprilTag）

> 文档版本：V1.0-A  
> 编写日期：2026-08-03  
> 适用项目：`Car_jetson`  
> 方案定位：在充电桩上刚性安装 AprilTag，通过标签 ID 识别目标充电桩，通过标签位姿计算车辆与充电面板的相对误差。  
> 对应方案：`CHARGING_DOCK_DEVELOPMENT_PLANB.md` 为无标记视觉检测、关键点模型和 PnP 方案。  
> 当前目标：在固定测试场地中，通过人工触发完成充电桩识别、车辆自动对准、低速靠近和停车。  
> 暂不包含：充电航点任务编排、充电桩供电控制、充电协议和充电完成后的任务恢复。

## 1. 项目背景

小车车尾已经安装充电接收面板，现场具有与之配套的充电桩发射面板。现有项目基于 ROS 2 Humble，已经接入 JD03 线控底盘、前后单线雷达、RoboSense 多线雷达、IMU、GNSS 和电池/BMS 状态反馈，并通过 `/cmd_vel -> cmdvel_2_ecu -> /ecu` 控制底盘。

多地图导航消息已经定义 `type=3` 为充电桩航点，`navigation_manager.py` 中也保留了充电点处理分支，但目前没有充电桩识别和精确对接实现。

方案 A 不再采集数据训练 YOLO 或其他目标检测、关键点模型，而是在每个充电桩上安装具有唯一 ID 的 AprilTag。系统使用相机内参、标签实际尺寸和 AprilTag 检测结果计算标签相对相机的六自由度位姿，再通过已标定的“标签到充电面板”刚性变换得到精确对接目标。

本阶段先把精确对接能力作为独立子系统开发，不依赖 Nav2、LIO-SAM、多地图导航或 MQTT 任务。完成独立验收后，再由 `NavigationManager` 在到达充电预对接航点时调用同一套 Dock Action。

## 2. 方案选择和项目目标

### 2.1 采用 AprilTag 的原因

相对于方案 B，方案 A 的主要优势是：

1. 不需要采集和标注 `1000~3000` 张充电桩训练图像。
2. 不需要训练、导出和维护 YOLO、ONNX 或 TensorRT 模型。
3. 标签 ID 可以直接映射到 `dock_id`，能够区分外观完全相同的多个充电桩。
4. 标签角点和实际尺寸已知，可以直接估计相对位姿。
5. 算法行为更确定，首版开发周期和算力需求更低。
6. 检测失败可以通过打印质量、安装位置、相机曝光和几何门控定位。

方案 A 的代价是充电桩必须允许安装并长期维护可见标签。如果现场不允许增加人工标记、标签容易被遮挡或外观要求不允许使用标签，则应改用方案 B。

### 2.2 本阶段目标

1. 将现有 USB 相机正式接入 ROS 2。
2. 完成相机内参标定以及相机到车体的外参标定。
3. 设计并制作适合工作距离的 AprilTag 标签或多标签板。
4. 为每个充电桩分配唯一标签 ID，并建立 `tag_id -> dock_id` 配置关系。
5. 稳定检测标签并获得标签相对相机的位姿。
6. 标定标签坐标系到桩端充电面板坐标系的刚性变换。
7. 根据纵向距离、横向偏差和航向偏差控制车辆低速倒车。
8. 在满足几何对准条件后停车并保持制动。
9. 对标签丢失、未知 ID、标签冲突、障碍物、控制超时和对接超时进行安全处理。
10. 通过人工命令启动、取消和重复执行对接测试。
11. 保留标准 Dock Action，便于后续接入充电航点逻辑。

### 2.3 本阶段不做的内容

1. 不训练 YOLO、姿态检测或关键点神经网络。
2. 不建立用于训练识别模型的大规模充电桩图像数据集。
3. 不要求在没有 AprilTag 的情况下识别充电桩。
4. 不修改路径规划，使车辆自动导航到充电区域。
5. 不处理 `type=3` 航点的自动触发逻辑。
6. 不控制充电桩通断电。
7. 不实现充电通信协议、充电时长控制和充满退出。
8. 不以 BMS `charge_status` 作为对接控制的必要输入。
9. 不使用充电桩地图绝对坐标替代实时相对位姿。

### 2.4 最终可交付能力

车辆被人工放置在允许的初始区域内后，操作人员发送一次对接命令，系统能够：

1. 找到目标 `dock_id` 对应的 AprilTag。
2. 拒绝未知标签、错误标签和配置冲突。
3. 计算桩端充电面板相对车尾接收面板的位置和方向。
4. 自动调整方向并倒车靠近。
5. 将车载接收面板与桩端发射面板对准。
6. 停车、制动并返回明确的成功或失败结果。

## 3. 当前项目基础与约束

### 3.1 已有可复用能力

| 能力 | 当前接口或位置 | 用途 |
| --- | --- | --- |
| JD03 底盘控制 | `/cmd_vel -> cmdvel_2_ecu -> /ecu` | 倒车、转向、停车 |
| 车辆状态 | `/vehicle_status` | 获取车速、转角、档位和驾驶模式 |
| BMS 状态 | `/battery_status` | 可选观察实际充电状态 |
| 后向单线雷达 | `/laser/scan_back` | 对接区域障碍物和粗距离冗余 |
| 融合雷达 | `/laser/scan_merge_laser` | 外围安全监控 |
| USB 相机测试 | `real_ws/src/driver/camera/camera_test.py` | 已能按 VID/PID 通过 V4L2/OpenCV 读取图像 |
| TF 体系 | `base_link` 及现有传感器坐标系 | 统一位姿变换 |
| 充电航点类型 | `Waypoint.type == 3` | 后续触发 Dock Action |

### 3.2 当前必须解决的问题

1. 相机尚未发布 ROS 图像和 `CameraInfo`。
2. 相机没有可靠的车尾固定安装和真实 TF。
3. 相机没有内参、畸变参数和标定文件。
4. 仓库中尚未集成 AprilTag ROS 2 检测包。
5. 充电桩没有标签板、标签 ID 规划和实际尺寸记录。
6. 标签到桩端充电面板的外参尚未测量。
7. 当前 `cmdvel_2_ecu` 的配置将小于 `LOW_SPEED_THRESH=0.8` 的非零速度强制抬升，不适合精细对接。
8. Nav2 和对接控制器如果同时发布速度，会形成双控制源。
9. 后向单线雷达最小有效距离约为 `0.35 m`，不能覆盖最后贴近阶段。
10. 底盘为双阿克曼/四轮转向模型，不能像差速车一样原地旋转或横移。
11. 车载接收面板与桩端发射面板的高度、俯仰和目标间距尚未形成标定数据。
12. 当前没有短距 ToF、机械接触开关或专用防撞触边接口。
13. 标签在近距离可能离开视野，必须在安装前验证完整对接轨迹中的可见性。
14. `navigation_manager.py` 当前先判断“是否最终航点”，再按航点类型分发；后续集成时最终充电航点不能直接结束任务。

### 3.3 核心原则

1. 标签必须与充电桩刚性连接，不能贴在可能移动、弯曲或振动的部件上。
2. 标签 ID 只负责身份，精确对接使用标签相对车辆的实时位姿。
3. 控制算法不得使用充电桩地图绝对坐标代替视觉闭环。
4. 标签检测、目标关联、位姿变换、运动控制和任务编排必须分层。
5. 任意关键输入失效时默认停车，不继续盲走。
6. 所有尺寸、外参和控制阈值必须进入配置文件，不在代码中硬编码。
7. 标签尺寸和外参必须实测，不使用打印文件的名义尺寸或手工估计值。
8. 调试显示不得成为安全控制链路的必要输入。

## 4. 推荐技术方案

### 4.1 总体技术路线

正式运行采用以下链路：

```text
后向相机图像 + CameraInfo
    -> 图像去畸变/校正
    -> AprilTag 检测
    -> 标签 ID 白名单和 dock_id 关联
    -> 标签角点/位姿质量门控
    -> camera -> tag 位姿
    -> tag -> dock_charge_link 标定变换
    -> base_link/receiver_link 下的对接误差
    -> Dock Action 状态机
    -> 低速倒车控制
```

第一版不自行实现 AprilTag 解码算法，优先采用经过验证、支持 ROS 2 Humble 的 `apriltag_ros` 和对应消息包。项目只实现适配、关联、标定变换、质量门控、滤波、控制和安全逻辑。

### 4.2 标签族、尺寸和制作要求

推荐初始使用 `tag36h11` 标签族。最终选择必须在目标相机、目标分辨率和实际光照下验证，不仅依据桌面测试。

标签制作要求：

1. 标签应安装在平整、刚性、哑光的板材上。
2. 保留 AprilTag 规范要求的完整白色静区，不裁切边缘。
3. 避免覆膜产生镜面反射；确需防水时使用低反光材料并重新验收。
4. 打印后实测检测器定义的标签边长，单位记录为米。
5. `tag_size` 的测量边界必须与所选检测包的尺寸定义一致。
6. 标签板不能安装在充电线圈有效面、活动盖板、电缆或可能形变的位置。
7. 标签板与发射面板之间的连接应有定位孔或机械基准，拆装后能够复位。
8. 标签污染、破损或翘曲必须进入日常检查清单。

标签边长暂不在文档中写死。首轮可制作多个候选尺寸，例如 `0.12 m`、`0.16 m` 和 `0.20 m`，通过 `0.3~2.0 m` 实测选择兼顾远距离像素尺寸和近距离视野的规格。

### 4.3 单标签和多标签板策略

首轮静态联调允许每个充电桩使用一个标签，以尽快验证相机、坐标系和控制方向。

正式实车方案优先使用一个刚性多标签板，原因是：

1. 单个标签局部遮挡后仍可能有其他标签可用。
2. 多组角点能够提高位姿估计稳定性。
3. 可以将标签布置在不同位置，扩大整个靠近过程中的可见范围。
4. 可通过多个 ID 的一致性检查排除错误关联。

多标签板要求：

1. 每个标签具有唯一 ID。
2. 所有标签到统一 `dock_tag_board_link` 的位姿经过测量或标定。
3. 每个标签的实际边长单独记录。
4. 同一标签 ID 不得在运行区域重复使用。
5. 不能简单平均多个标签各自输出的欧拉角和位置。
6. 优先使用支持标签板联合求解的接口；否则将有效角点转换到统一板坐标系后进行一次几何求解。
7. 如果只剩单个标签可见，允许降级运行的距离和速度必须单独配置。

近距离可见性不足时，可以在同一刚性支架上设置“靠近标签”和“最终标签”。二者均映射到同一 `dock_id`，但必须通过标定变换归一到同一 `dock_charge_link`。标签切换前后目标位姿跳变超限时必须停车，不能直接继续控制。

### 4.4 标签 ID 和充电桩身份

方案 A 使用标签 ID 直接建立物理身份，避免从外观猜测充电桩编号。

约定：

1. `dock_id=0` 为单桩调试模式，只接受配置中唯一可用的充电桩。
2. 正式模式使用 `dock_id > 0`。
3. 每个 `dock_id` 对应一组允许的 `tag_id`。
4. 检测到不在白名单内的标签时只记录，不生成可执行目标。
5. 同一 `tag_id` 配置到多个充电桩时，配置校验必须阻止节点启动。
6. 同时检测到属于不同充电桩的标签时，只选择 Action 指定的 `dock_id`。
7. 指定充电桩的标签与其他充电桩标签在几何上混入同一标签板时，报告配置或现场异常。

标签 ID 解决的是身份问题，不替代障碍物检查、位姿质量门控或运动边界。

### 4.5 相机和图像链路

建议使用系统提供的 `v4l2_camera` 或 `usb_cam` 发布 ROS 图像，不在 `charging_dock` 包内重复实现底层相机采集。

要求：

1. 通过 `/dev/v4l/by-id`、udev 规则或稳定序列号锁定设备，不能依赖易变化的 `/dev/videoX`。
2. 固定运行分辨率、帧率、像素格式、曝光和白平衡策略。
3. 发布 `/rear_camera/image_raw` 和 `/rear_camera/camera_info`。
4. 检测器使用与标定一致的图像分辨率。
5. 优先对去畸变后的图像检测；若检测器直接使用原图，必须确认其位姿求解正确处理畸变。
6. 图像时间戳必须来自采集链路，禁止在下游收到图像时重新打时间戳。
7. 检测节点不得处理积压旧帧；控制使用最新有效观测。

### 4.6 AprilTag 检测器集成

建议将第三方检测器作为独立 ROS 2 节点运行，再由 `dock_tag_adapter.py` 适配为项目内部稳定消息。

检测器配置至少包括：

1. 标签族，例如 `tag36h11`。
2. 允许的标签 ID。
3. 每个标签实际边长。
4. 线程数。
5. 图像降采样参数。
6. 边缘细化和解码参数。
7. 是否发布调试图像和 TF。
8. 输入图像、`CameraInfo` 和输出话题名称。

依赖管理要求：

1. 固定使用的 `apriltag_ros` 来源、版本或 Git 提交号。
2. 优先使用发行版二进制包；需要源码构建时记录仓库和提交号。
3. 不把未经审核的第三方输出直接当作控制指令。
4. 不依赖检测器自动发布的动态 TF 名称作为长期接口，内部适配层应明确 frame 和时间戳。
5. 不同 `apriltag_ros` 版本的消息字段可能不同，项目内部消息应隔离这种差异。

### 4.7 位姿估计和质量门控

AprilTag 检测器根据二维角点、相机内参和标签实际尺寸输出标签相对相机的位姿。项目内部仍须执行二次质量门控。

每帧检查至少包括：

1. 标签族和 ID 是否在目标充电桩白名单内。
2. 解码纠错位数是否在允许范围内，首版建议只接受无纠错或最严格配置。
3. 决策质量或等价指标是否达到阈值。
4. 四个角点是否位于图像有效区域，是否过于靠近边缘。
5. 标签像素边长和面积是否满足当前工作距离要求。
6. 重投影误差是否低于阈值。
7. 标签是否在相机前方，距离是否处于允许范围。
8. 标签法向是否朝向车辆，是否出现不合理翻转。
9. 与上一帧同一标签的平移和旋转跳变是否合理。
10. 图像和检测结果时间戳是否新鲜。
11. 多标签观测变换到统一板坐标系后是否互相一致。

如果第三方消息不提供重投影误差或质量值，适配层应保存角点并用相机模型重新计算，而不是省略几何检查。

### 4.8 标签到充电面板的外参

AprilTag 的中心通常不等于充电面板中心，标签平面也不一定与发射面板完全共面。必须建立：

```text
dock_tag_board_link -> dock_charge_link
```

标定要求：

1. 定义 `dock_charge_link` 原点为桩端发射面板几何中心。
2. 明确定义各坐标轴方向和面板法向。
3. 使用卡尺、治具、机械图纸或标定板测量标签板到充电面板的三维平移和旋转。
4. 对非共面安装同时记录横滚、俯仰和偏航，不能只记录二维偏移。
5. 每个充电桩如果机械安装不完全一致，应允许使用独立外参。
6. 拆装标签板后必须复检外参。
7. 使用静态测量基准对最终 `dock_charge_link` 位姿进行独立验证。

控制器不直接追踪标签中心，而是追踪由该外参计算出的 `dock_target_link`。

### 4.9 多充电桩候选关联

单桩独立测试模式：

1. 测试区域只配置一个 `dock_id`。
2. 必须检测到属于该充电桩的有效标签。
3. 检测到其他已注册充电桩标签时报告现场不符合单桩条件。
4. 未知标签不参与控制。

正式多桩模式：

1. 使用 `waypoint.id -> dock_id` 建立任务关系。
2. Dock Action 只接受目标 `dock_id` 对应的标签组。
3. 标签 ID 是主要身份来源，地图位置只用于合理性校验。
4. 同时看到多个充电桩时不按图像面积或置信度猜测目标。
5. 同一充电桩的多个标签必须通过标签板几何一致性检查。
6. 检测到 ID 冲突、重复标签或标签板几何不一致时停车并返回错误。

### 4.10 系统数据流

```text
后向 USB 相机
    -> /rear_camera/image_raw + /rear_camera/camera_info
    -> 图像校正
    -> /rear_camera/image_rect
    -> apriltag_ros
    -> /apriltag/detections
    -> 标签适配、ID 白名单和标签板联合位姿
    -> /charging_dock/tag_observations
    -> tag/board -> dock_charge_link 标定变换
    -> /charging_dock/dock_pose
    -> base_link/receiver_link 下的位姿变换与滤波
    -> 纵向/横向/航向误差
    -> Dock Action Server 状态机
    -> /cmd_vel_dock
    -> 速度指令仲裁器
    -> /cmd_vel_selected
    -> cmdvel_2_ecu 对接低速模式
    -> /ecu
    -> JD03 底盘
```

辅助输入：

```text
/vehicle_status       -> 实际车速、档位、转角和自动驾驶状态
/laser/scan_back      -> 后向障碍物和视觉距离粗校验
/battery_status       -> 对接完成后的可选充电状态观察
/tf                   -> camera、receiver、base_link 坐标变换
目标 dock_id          -> 选择配置中对应的 AprilTag 标签组
```

### 4.11 坐标系设计

至少定义以下坐标系：

| 坐标系 | 定义 |
| --- | --- |
| `base_link` | 车辆控制参考中心，遵循项目现有定义 |
| `receiver_link` | 车载充电接收面板的几何中心和法向 |
| `rear_camera_link` | 后向相机机体坐标系 |
| `rear_camera_optical_frame` | ROS 光学坐标系 |
| `dock_tag_<id>` | 单个 AprilTag 坐标系 |
| `dock_tag_board_link` | 同一充电桩多标签板的统一坐标系 |
| `dock_charge_link` | 桩端充电面板中心和法向 |
| `dock_target_link` | 完成对接时 `receiver_link` 应到达的目标位姿 |

位姿计算链：

```text
base_link -> rear_camera_link -> rear_camera_optical_frame
rear_camera_optical_frame -> dock_tag_<id> -> dock_tag_board_link
dock_tag_board_link -> dock_charge_link -> dock_target_link
base_link -> receiver_link
```

控制器比较 `receiver_link` 与 `dock_target_link`，输出：

1. 纵向剩余距离 `e_longitudinal`。
2. 横向中心偏差 `e_lateral`。
3. 平面航向误差 `e_yaw`。

横滚、俯仰和垂直高度作为机械安装检查和位姿门控输入，第一版底盘控制只修正平面三自由度。

### 4.12 近距离观测策略

最后 `0.20~0.30 m` 是方案 A 的关键风险区。进入实车闭环前必须完成视场验证：

1. 沿完整倒车轨迹记录标签角点与图像边界的距离。
2. 确认最终停车位置仍有至少一个允许用于控制的标签完整可见。
3. 如果标签会离开视野，调整相机俯仰、焦距、安装高度或标签板位置。
4. 必要时增加专用近距标签，但必须与同一充电面板刚性标定。
5. 不允许在标签已不可见后按上一次位姿持续倒车到接触。
6. 若最终机械结构不可避免遮挡标签，应增加短距 ToF、接触开关或其他可验证的最终阶段传感器，再单独定义切换条件；该扩展不在首版默认范围内。

## 5. 软件包和文件规划

建议在 `real_ws/src` 中增加两个独立 ROS 2 包：

```text
real_ws/src/
├── charging_dock_msgs/
│   ├── action/
│   │   └── Dock.action
│   ├── msg/
│   │   ├── DockTagObservation.msg
│   │   ├── DockTagObservationArray.msg
│   │   └── DockState.msg
│   ├── CMakeLists.txt
│   └── package.xml
└── charging_dock/
    ├── charging_dock/
    │   ├── dock_tag_adapter.py
    │   ├── dock_tag_selector.py
    │   ├── dock_board_pose_estimator.py
    │   ├── dock_pose_transformer.py
    │   ├── dock_pose_filter.py
    │   ├── dock_controller.py
    │   ├── dock_action_server.py
    │   ├── command_mux.py
    │   └── safety_monitor.py
    ├── config/
    │   ├── camera_info.yaml
    │   ├── apriltag_detector.yaml
    │   ├── tag_layout.yaml
    │   ├── dock_registry.yaml
    │   ├── dock_geometry.yaml
    │   ├── controller.yaml
    │   └── safety.yaml
    ├── tools/
    │   ├── validate_tag_registry.py
    │   ├── evaluate_tag_pose.py
    │   ├── calibrate_tag_to_dock.py
    │   └── check_tag_visibility.py
    ├── launch/
    │   ├── rear_camera.launch.py
    │   ├── apriltag_docking.launch.py
    │   └── docking_test.launch.py
    ├── test/
    │   ├── test_tag_registry.py
    │   ├── test_tag_selection.py
    │   ├── test_board_pose.py
    │   ├── test_pose_transform.py
    │   ├── test_pose_filter.py
    │   ├── test_controller.py
    │   └── test_state_machine.py
    ├── package.xml
    └── setup.py
```

第三方 `apriltag_ros` 保持独立依赖，不复制其核心算法到 `charging_dock`。项目内部只依赖稳定的适配消息，避免检测器版本变化扩散到控制层。

## 6. 配置和 ROS 接口设计

### 6.1 Dock Action

第一版建议定义：

```text
# Goal
int32 dock_id
float32 timeout_sec
bool dry_run
---
# Result
bool success
uint8 error_code
string message
float32 final_lateral_error
float32 final_longitudinal_error
float32 final_yaw_error
---
# Feedback
uint8 state
float32 lateral_error
float32 longitudinal_error
float32 yaw_error
float32 detection_quality
int32 active_tag_id
```

`dry_run=true` 时只运行标签检测、身份关联、位姿计算和状态机，不向底盘发送非零速度。

`dock_id` 约定：

1. `dock_id=0` 为单桩独立调试模式。
2. 正式多桩阶段使用大于零的固定 ID。
3. Action 接收后锁定目标 `dock_id`，运行中不能因看到其他标签而自动切换充电桩。

### 6.2 标签注册表示例

`dock_registry.yaml` 建议表达物理身份，`tag_layout.yaml` 表达刚性几何。以下只展示字段结构，尺寸和外参必须由实测填写：

```yaml
docks:
  1:
    enabled: true
    tag_family: tag36h11
    tag_ids: [11, 12, 13]
    primary_tag_id: 11
    board_frame: dock_1_tag_board_link
    charge_frame: dock_1_charge_link

tags:
  11:
    dock_id: 1
    size_m: 0.16
    xyz_in_board_m: [0.0, 0.0, 0.0]       # 示例占位，必须实测
    rpy_in_board_rad: [0.0, 0.0, 0.0]     # 示例占位，必须实测
  12:
    dock_id: 1
    size_m: 0.12
    xyz_in_board_m: [0.0, 0.0, 0.0]       # 示例占位，必须实测
    rpy_in_board_rad: [0.0, 0.0, 0.0]     # 示例占位，必须实测
```

启动时必须校验：

1. ID 是否重复。
2. 标签尺寸是否为正。
3. 每个 `dock_id` 是否至少存在一个标签。
4. 主标签是否属于该充电桩。
5. 所有变换是否为有限数值且旋转合法。
6. 配置 frame 名称是否唯一。

### 6.3 计划话题

| 话题 | 类型 | 方向 | 说明 |
| --- | --- | --- | --- |
| `/rear_camera/image_raw` | `sensor_msgs/Image` | 输入 | 后向相机原始图像 |
| `/rear_camera/camera_info` | `sensor_msgs/CameraInfo` | 输入 | 相机内参和畸变参数 |
| `/rear_camera/image_rect` | `sensor_msgs/Image` | 中间输入 | 校正后图像 |
| `/apriltag/detections` | 检测器消息 | 中间输入 | 第三方 AprilTag 原始结果 |
| `/charging_dock/tag_observations` | 自定义数组 | 中间输出 | 已适配的标签 ID、角点、质量和位姿 |
| `/charging_dock/dock_pose` | `geometry_msgs/PoseWithCovarianceStamped` | 中间输出 | 桩端充电面板位姿 |
| `/charging_dock/target_pose` | `geometry_msgs/PoseStamped` | 中间输出 | 最终对接目标相对车辆的位姿 |
| `/charging_dock/state` | 自定义状态消息 | 输出 | 当前状态、标签 ID 和故障码 |
| `/cmd_vel_dock` | `geometry_msgs/Twist` | 输出 | 对接控制速度 |
| `/cmd_vel_nav` | `geometry_msgs/Twist` | 输入 | Nav2 独立速度通道 |
| `/cmd_vel_selected` | `geometry_msgs/Twist` | 输出 | 仲裁后唯一底盘速度 |

第三方检测话题的精确消息类型以选定 `apriltag_ros` 版本为准，不把版本相关类型直接暴露给 Action 和控制器。

### 6.4 错误码建议

| 错误码 | 含义 |
| --- | --- |
| `0` | 成功 |
| `1` | 未找到目标充电桩标签 |
| `2` | 标签在运动过程中丢失 |
| `3` | 对接超时 |
| `4` | 后方存在非预期障碍物 |
| `5` | 车辆状态不可用或超时 |
| `6` | 控制输出超时 |
| `7` | 初始位置超出允许范围 |
| `8` | 多次重试后仍无法对准 |
| `9` | 操作人员取消或急停 |
| `10` | 标签 ID 未注册、重复或与目标 dock_id 不匹配 |
| `11` | AprilTag 检测节点未就绪或输出无效 |
| `12` | 重投影误差、法向或标签板几何检查不通过 |
| `13` | 标签配置或标签到充电面板外参无效 |
| `14` | 标签切换前后目标位姿不一致 |

## 7. 对接状态机设计

```text
IDLE
  -> ACQUIRE_TAG
  -> VALIDATE_INITIAL_POSE
  -> ALIGN
  -> APPROACH
  -> FINAL_APPROACH
  -> VERIFY
  -> DOCKED
```

任何运动状态均可进入：

```text
STOPPING -> RETRY 或 FAILED
```

### 7.1 状态说明

#### IDLE

1. 输出零速度并保持制动。
2. 等待 Dock Action 请求。
3. 检查相机、`CameraInfo`、AprilTag 节点、TF、车辆状态和后雷达新鲜度。
4. 校验标签注册表和外参配置。

#### ACQUIRE_TAG

1. 读取视野内全部 AprilTag。
2. 只保留目标 `dock_id` 对应的 ID。
3. 要求同一充电桩连续若干帧身份、角点和位姿有效。
4. 检查解码质量、角点边界、重投影误差、法向、距离和时间戳。
5. 多标签可见时检查标签板几何一致性。
6. 未找到目标、出现 ID 冲突或配置歧义时返回失败。
7. 不允许通过盲目倒车搜索标签。

#### VALIDATE_INITIAL_POSE

检查：

1. 纵向距离在初始允许范围内。
2. 横向和航向误差没有超出阿克曼底盘可恢复范围。
3. 车辆当前速度接近零。
4. 自动驾驶控制已使能。
5. 目标标签在当前图像中具有足够可见裕量。
6. 充电桩面板高度和俯仰没有超出机械容差。

#### ALIGN

1. 以较低倒车速度沿可行圆弧修正横向和航向误差。
2. 控制命令符合双阿克曼运动约束。
3. 不尝试横移或原地旋转。
4. 标签丢失、位姿跳变或误差持续增大时立即停车。

#### APPROACH

1. 随距离缩短逐步降低速度。
2. 持续修正小范围横向和航向误差。
3. 多标签板允许按质量选择或联合估计，但目标必须保持在同一 `dock_charge_link`。
4. 后雷达用于检测非预期障碍物和距离粗校验。

#### FINAL_APPROACH

1. 最后约 `0.20~0.30 m` 保持近似直线倒车。
2. 限制最大转角、最大速度和单次运动距离。
3. 必须持续获得当前阶段允许标签的新鲜观测。
4. 标签即将离开图像、被遮挡或质量低于阈值时停车。
5. 横向或航向误差超限时，停车后前移至安全距离重试。
6. 不允许在充电桩前大转角强行纠偏。

#### VERIFY

1. 连续多帧检查最终三项误差。
2. 发布零速度并确认 `/vehicle_status` 中实际车速归零。
3. 保持制动一定时间。
4. 检查最后有效观测的数据年龄。
5. 可选观察 `/battery_status.charge_status`，但不影响本阶段几何验收。

#### DOCKED

1. 返回成功结果、最终误差和最后使用的标签 ID。
2. 保持零速度和制动。
3. 未收到退出命令前不释放运动控制权。

#### RETRY

1. 停车确认后，低速前移至安全重试距离。
2. 重新进入 `ACQUIRE_TAG`。
3. 重试次数达到上限后进入 `FAILED`。

## 8. 控制策略

### 8.1 控制输入

控制器使用滤波后的：

```text
e_longitudinal
e_lateral
e_yaw
```

控制循环建议从 `20 Hz` 开始，但不得高于有效标签位姿更新率。每个控制周期必须检查观测时间戳。

### 8.2 控制输出

输出标准 ROS `Twist`：

1. `linear.x < 0` 表示倒车。
2. `angular.z` 根据现有 `cmdvel_2_ecu` 符号约定生成。
3. 当前转换节点内部使用 `omega = -msg.angular.z`，实车测试仍必须验证倒车左右转实际方向。
4. 控制器只发布 `/cmd_vel_dock`，不直接发布 `/ecu`。

### 8.3 速度分段初值

以下只作为联调初值，必须在底盘低速测试后修改：

| 剩余距离 | 候选倒车速度 |
| --- | --- |
| 大于 `0.60 m` | `0.10~0.15 m/s` |
| `0.20~0.60 m` | `0.05~0.08 m/s` |
| 小于 `0.20 m` | `0.02~0.04 m/s` 或脉冲移动 |

如果 ECU 无法稳定执行连续微速，则采用：

```text
短时间移动 -> 刹车 -> 等待车体稳定 -> 获取新标签位姿 -> 再移动
```

脉冲移动必须限制单次持续时间、累计距离和最大次数。

### 8.4 位姿滤波

第一版建议组合使用：

1. 标签身份和几何有效性检查。
2. 连续帧平移和旋转跳变阈值。
3. 短窗口中值滤波。
4. 指数低通或 One Euro Filter。
5. 多标签观测一致性检查。
6. 时间戳和数据新鲜度检查。

滤波不得掩盖标签切换、ID 冲突、位姿翻转或观测失效。超过规定时间没有目标充电桩的有效位姿，必须立即停车。

## 9. 速度指令仲裁与底盘改造

### 9.1 目标结构

```text
Nav2        -> /cmd_vel_nav
Dock 控制器 -> /cmd_vel_dock
                    |
                    v
              command_mux
                    |
                    v
          /cmd_vel_selected
                    |
                    v
              cmdvel_2_ecu
```

仲裁要求：

1. 同一时刻只允许一个控制源。
2. Dock Action 有效期间对接控制源优先。
3. 任意控制源超时后立即输出零速度。
4. 控制权切换时先输出零速度并确认车辆停车。
5. 对接控制器退出时连续发布若干次零速度。
6. 急停和安全监控优先级高于 Nav2 与 Dock。

### 9.2 `cmdvel_2_ecu` 改造要求

1. 增加正常导航和精确对接两套速度限制参数。
2. 对接模式不使用当前 `LOW_SPEED_THRESH=0.8` 的强制下限。
3. 对接模式限制最大倒车速度、最大转角和加速度变化。
4. 对接模式下输入超时必须制动。
5. 明确 `/cmd_vel` 的单位与 ECU 实际目标速度单位。
6. 使用实车数据验证倒车时 `angular.z -> steer` 的符号和比例。
7. 修正速度偏置逻辑时必须覆盖负速度，当前实现的条件分支需要实车和单元测试确认。

不建议让对接控制器直接发布 `/ecu`，否则会绕过现有转换逻辑并产生多个 ECU 控制源。

## 10. 分阶段开发计划

### 阶段 0：硬件基线和安全准备

任务：

- [ ] 确认 USB 相机型号、设备节点、稳定序列号和最大稳定帧率。
- [ ] 确认相机能够刚性安装在车尾并持续看到目标标签区域。
- [ ] 测量车载和桩端充电面板的尺寸、中心、高度和允许间距。
- [ ] 确认人工遥控急停可用。
- [ ] 清理测试区域并设置车辆最大活动边界。
- [ ] 实测底盘最小稳定倒车速度和制动距离。
- [ ] 确认 `/vehicle_status` 反馈频率和车速零点。
- [ ] 确认 `/laser/scan_back` 数据方向和有效范围。
- [ ] 获取充电桩允许安装标签板的位置和机械基准。

完成标志：形成硬件测量记录和低速测试记录；任何自动运动前急停和零速度保护已验证。

### 阶段 1：AprilTag 标签板设计和视场验证

任务：

- [ ] 确定标签族，首选 `tag36h11`。
- [ ] 制作多个候选尺寸的哑光标签。
- [ ] 为测试充电桩分配唯一 `dock_id` 和标签 ID。
- [ ] 设计单标签和多标签板安装方案。
- [ ] 确认标签板不会干扰充电面板、电气结构或维护操作。
- [ ] 在 `0.3~2.0 m`、不同横向和航向偏差下检查像素尺寸。
- [ ] 模拟完整靠近轨迹，确认最终阶段标签不会离开视野。
- [ ] 测试明亮、昏暗、逆光、反光、局部遮挡和污染情况。
- [ ] 固定标签尺寸、板材、打印文件、打印比例和安装位置。

完成标志：形成标签板图纸、实测尺寸、ID 清单和视场测试记录；完整对接轨迹至少有一个有效标签保持可见。

### 阶段 2：相机 ROS 化与内参标定

任务：

- [ ] 使用稳定设备路径锁定相机。
- [ ] 固定运行分辨率和帧率，初始可测试 `1280x720@30fps`。
- [ ] 发布 `/rear_camera/image_raw` 和 `/rear_camera/camera_info`。
- [ ] 使用棋盘格或 ChArUco 完成相机内参标定。
- [ ] 保存标定日期、相机序列号、分辨率和标定板尺寸。
- [ ] 检查去畸变图像和标定重投影误差。
- [ ] 固定或限制会影响检测稳定性的曝光参数。
- [ ] 完成持续运行和断连恢复测试。

完成标志：相机稳定发布至少 `20 Hz`；运行分辨率与标定分辨率一致；标定重投影误差目标小于 `0.5 px`。

### 阶段 3：AprilTag ROS 2 集成

任务：

- [ ] 选定支持 ROS 2 Humble 的 `apriltag_ros` 来源和版本。
- [ ] 安装或源码构建并记录版本。
- [ ] 配置标签族、尺寸、ID 和检测参数。
- [ ] 验证检测消息的 frame、时间戳、角点顺序和位姿定义。
- [ ] 实现 `dock_tag_adapter.py`，隔离第三方消息差异。
- [ ] 发布标签 ID、角点、质量、位姿和数据年龄。
- [ ] 对未知 ID、重复 ID 和无效配置执行启动校验。
- [ ] 测量 Jetson 上检测频率、端到端延迟、CPU/内存占用和温度。
- [ ] 完成长时间运行和相机断连恢复测试。

完成标志：在目标 Jetson 和工作距离内稳定输出目标标签观测，第三方检测消息不直接进入控制器。

### 阶段 4：标签板外参、目标位姿和 Dry Run

任务：

- [ ] 标定或测量 `base_link -> rear_camera_link`。
- [ ] 标定或测量 `base_link -> receiver_link`。
- [ ] 测量每个标签到 `dock_tag_board_link` 的变换。
- [ ] 测量 `dock_tag_board_link -> dock_charge_link`。
- [ ] 配置 `dock_charge_link -> dock_target_link` 的目标间距。
- [ ] 实现标签 ID 到 `dock_id` 的关联。
- [ ] 实现解码质量、角点、重投影误差、距离和法向门控。
- [ ] 实现多标签板联合位姿或一致性选择。
- [ ] 实现完整 TF 变换链和三项平面误差。
- [ ] 在 RViz 中显示标签、标签板、充电面板、目标位姿和误差方向。
- [ ] 实现 `dry_run=true`，只输出建议控制量而不动车。
- [ ] 人工改变车体位置，确认控制方向始终正确。
- [ ] 使用测量治具评估最终 `dock_charge_link` 位姿误差。
- [ ] 为注册表、标签选择、位姿变换、角度归一化和误差符号增加单元测试。

完成标志：不启动车辆即可完整执行 Dock Action，目标身份正确，输出的控制方向与人工判断一致。

### 阶段 5：底盘低速控制与指令仲裁

任务：

- [ ] 增加 `/cmd_vel_nav` 和 `/cmd_vel_dock` 控制通道。
- [ ] 实现带优先级、超时和零速度切换的命令仲裁器。
- [ ] 为 `cmdvel_2_ecu` 增加对接低速模式。
- [ ] 验证前进、倒车、左转、右转和刹车指令。
- [ ] 测量不同低速指令的实际车速和停车距离。
- [ ] 验证控制节点退出或崩溃时车辆自动停车。
- [ ] 确定连续微速或脉冲移动方案。

完成标志：人工发送 `/cmd_vel_dock` 时车辆只能受限低速运动；停止发布后在规定时间内制动；Nav2 不能同时获得控制权。

### 阶段 6：闭环对准和靠近

按风险从低到高逐步开放：

1. 只允许直线倒车和距离停车。
2. 加入小航向误差修正。
3. 加入小横向偏差修正。
4. 加入分段降速。
5. 验证多标签到近距标签的观测切换。
6. 加入最终直线靠近。
7. 加入退出重试和失败处理。

第一轮范围：

| 参数 | 范围 |
| --- | --- |
| 初始距离 | `1.0~1.5 m` |
| 横向偏差 | `±0.10 m` |
| 航向偏差 | `±5 deg` |
| 最大倒车速度 | 由阶段 5 实测决定，且不超过 `0.15 m/s` |
| 重试次数 | `0~1` 次 |

稳定后扩展到：

| 参数 | 目标 |
| --- | --- |
| 初始距离 | `0.5~2.0 m` |
| 横向偏差 | `±0.30 m` |
| 航向偏差 | `±15 deg` |
| 重试次数 | 最多 `2~3` 次 |

完成标志：车辆从第一轮范围内自动完成对准和停车；标签丢失、错误 ID 或取消操作都会停车。

### 阶段 7：安全和异常测试

任务：

- [ ] 遮挡单个标签和整个标签板。
- [ ] 使标签部分离开画面。
- [ ] 放置未注册 AprilTag。
- [ ] 放置属于其他充电桩的有效标签。
- [ ] 模拟重复标签 ID 和注册表冲突。
- [ ] 使用错误尺寸配置，验证几何门控能够发现异常。
- [ ] 模拟标签板松动、倾斜和外参错误。
- [ ] 模拟位姿翻转、重投影误差超限和标签切换跳变。
- [ ] 停止检测节点、停止相机或拔掉相机。
- [ ] 停止 `/vehicle_status`。
- [ ] 在后方放置非充电桩障碍物。
- [ ] 人工取消 Dock Action。
- [ ] 杀死对接控制节点。
- [ ] 模拟 TF 缺失或时间戳过期。
- [ ] 让初始位姿超出允许范围。
- [ ] 模拟车辆无运动、打滑或误差持续增大。
- [ ] 验证最大运动距离、最大执行时间和最大重试次数。

完成标志：所有异常均返回明确错误码并停车，不出现盲走、控制权残留或切换到错误充电桩。

### 阶段 8：重复性验收

进行至少 50 次独立对接测试，覆盖：

1. 不同初始距离。
2. 左右横向偏差。
3. 正负航向偏差。
4. 明亮、普通和较暗照明。
5. 局部遮挡、标签边缘入画和背景变化。
6. 不同电池电量和车辆负载。
7. 未知标签和其他充电桩标签。
8. 单标签可见和多标签可见状态。

每次记录：

1. 是否成功。
2. 目标 `dock_id` 和使用的标签 ID。
3. 总执行时间和重试次数。
4. 最终三项误差。
5. 最大速度和制动距离。
6. 失败阶段和错误码。
7. 检测质量、角点像素尺寸和重投影误差。
8. 感知延迟和观测数据年龄。
9. 标签切换次数及切换前后位姿差。

完成标志：满足第 12 节验收标准，并形成测试报告和 rosbag 数据集。

## 11. 测试与数据记录

### 11.1 建议录制话题

```bash
ros2 bag record \
  /rear_camera/image_raw \
  /rear_camera/camera_info \
  /apriltag/detections \
  /charging_dock/tag_observations \
  /charging_dock/dock_pose \
  /charging_dock/target_pose \
  /charging_dock/state \
  /cmd_vel_dock \
  /cmd_vel_selected \
  /vehicle_status \
  /battery_status \
  /laser/scan_back \
  /tf \
  /tf_static
```

### 11.2 测试分层

1. 配置测试：标签 ID 唯一性、尺寸、外参和 frame 校验。
2. 单元测试：标签选择、坐标变换、角度归一化、滤波、状态机和控制限幅。
3. 静态位姿评估：使用测量治具验证不同距离和角度下的误差。
4. rosbag 离线回放：验证关联、位姿门控、滤波和控制计算，不连接底盘。
5. Dry Run：连接实车传感器但禁止非零速度。
6. 低速开环：人工发送受限速度，验证底盘响应。
7. 闭环分段：直线、航向、横向和最终靠近逐步开放。
8. 故障注入：遮挡、错误 ID、断节点、旧时间戳、障碍物和外参异常。
9. 重复性验收：固定测试矩阵并统计成功率。

### 11.3 必须保存的信息

1. 相机设备序列号和稳定设备路径。
2. 相机标定原图、标定板参数和 `camera_info.yaml`。
3. AprilTag 生成文件、标签族、ID、打印比例、板材和实测边长。
4. 标签板机械图纸、安装照片和版本号。
5. 每个标签在 `dock_tag_board_link` 下的实测位姿。
6. `dock_tag_board_link -> dock_charge_link` 的测量方法和数值。
7. `apriltag_ros` 来源、版本、构建方式和参数。
8. 所有静态 TF 的测量方法和数值。
9. 充电面板允许的实际对准误差。
10. 底盘最小稳定速度和刹停距离。
11. 每次测试使用的 Git 提交号和参数文件。
12. 成功与失败 rosbag。

## 12. 验收标准

以下为第一版工程目标，最终应以充电硬件厂家提供的允许偏差为准。

### 12.1 相机和 AprilTag

- [ ] 相机持续输出不低于 `20 Hz`。
- [ ] 运行分辨率与标定分辨率一致。
- [ ] 相机标定重投影误差目标小于 `0.5 px`。
- [ ] 标签族、ID、实际尺寸和安装外参均有可追溯记录。
- [ ] `0.3~2.0 m` 工作范围内能够稳定识别目标标签。
- [ ] 横向 `±0.30 m`、航向 `±15 deg` 范围内满足有效观测要求。
- [ ] 目标 `dock_id` 与标签 ID 关联正确率为 `100%`。
- [ ] 未知 ID、其他充电桩 ID 和冲突配置不会生成可执行目标。
- [ ] Jetson 端有效位姿输出不低于 `15 Hz`。
- [ ] 感知链路 95 分位延迟目标不超过 `150 ms`。
- [ ] `1.0 m` 内视觉位姿横向 RMSE 目标不超过 `10 mm`。
- [ ] `1.0 m` 内视觉位姿航向 RMSE 目标不超过 `1 deg`。
- [ ] 多标签切换后 `dock_charge_link` 不出现导致车辆误动作的跳变。
- [ ] 标签丢失能够在 `200 ms` 量级触发停车流程。
- [ ] 最终停车位置仍有满足控制要求的新鲜标签观测。

### 12.2 最终对准

- [ ] 横向误差目标不超过 `±20 mm`。
- [ ] 航向误差目标不超过 `±2 deg`。
- [ ] 面板间距进入实测目标值的 `±10 mm` 窗口。
- [ ] 车辆实际速度确认归零并保持制动。
- [ ] 最终阈值连续满足规定帧数后才返回成功。
- [ ] 返回结果包含实际目标 `dock_id` 和最后使用的标签 ID。

### 12.3 安全和可靠性

- [ ] 50 次测试总体成功率不低于 `95%`。
- [ ] 无碰撞、无越界和无失控运动。
- [ ] 取消、标签丢失、错误 ID、输入超时和节点异常均能停车。
- [ ] 任意时刻只有一个速度控制源。
- [ ] 达到最大时间、距离或重试次数后明确失败。
- [ ] 失败后车辆保持静止，不自动恢复运动。
- [ ] 标签板松动、外参异常或几何不一致时禁止运动。

## 13. 主要风险及应对

| 风险 | 影响 | 应对措施 |
| --- | --- | --- |
| 标签尺寸配置错误 | 距离和位姿按比例错误 | 实测边长；配置启动校验；用治具验证多个距离 |
| 打印缩放或板材变形 | 角点几何不准确 | 禁止“适应页面”缩放；刚性板材；记录实测尺寸 |
| 标签反光、污损或褪色 | 漏检和位姿跳变 | 哑光材料；维护检查；覆盖极端照明测试 |
| 标签部分遮挡 | 单标签丢失 | 使用刚性多标签板；质量降级；丢失即停 |
| 近距离标签离开视野 | 最终阶段失去闭环 | 轨迹视场验证；调整相机和标签；增加近距标签 |
| 标签 ID 重复 | 对接错误充电桩 | 全局注册表唯一性校验；现场巡检；目标 ID 锁定 |
| 检测到未知标签 | 错误关联 | ID 白名单；未知标签只记录不控制 |
| 单标签位姿翻转 | 控制方向错误 | 法向、重投影、时序和板几何门控 |
| 标签板外参错误或松动 | 标签对准但充电面板错位 | 刚性定位；拆装复检；外参版本管理 |
| 相机支架振动或移动 | 相机外参失效 | 刚性安装、定位销、测试前基准检查 |
| 相机曝光或运动模糊 | 检测率下降 | 固定曝光范围、补光、限制速度和快门 |
| Jetson 处理延迟 | 控制使用过期位姿 | 处理最新帧、监控数据年龄、超时即停 |
| 当前最低速度过高 | 最终位置超调 | 对接低速模式；分段降速；脉冲移动 |
| 双阿克曼不能横移 | 大横向偏差难以修正 | 远距离完成纠偏；超限前移重试 |
| 后雷达存在近距盲区 | 最后阶段防撞不足 | 极低速、视觉距离、运动上限、人工急停；必要时增加短距传感器 |
| Nav2 与 Dock 同时发速度 | 指令冲突 | 强制命令仲裁和控制权状态机 |
| 垂直高度或俯仰不一致 | 平面对准但无法充电 | 通过机械结构调节；软件门控但不强行补偿 |
| 充电硬件容差未知 | 验收阈值不可靠 | 获取厂家规格或通过静态通电试验测量有效窗口 |

## 14. 后续接入充电航点的预留方案

本阶段验收后，再进行以下集成：

1. 将 `type=3` 航点的 `x/y/yaw` 定义为充电桩预对接车体位姿，而不是标签或充电桩实体中心。
2. 使用 `Waypoint.id` 或新增字段建立 `waypoint -> dock_id` 关系。
3. 为每个固定充电桩配置允许的标签 ID、地图近似位姿和关联检查半径。
4. Nav2 只负责到达预对接位。
5. `NavigationManager` 在 Nav2 成功后将目标 `dock_id` 传给 Dock Action。
6. Dock 节点使用标签 ID 确认物理充电桩，地图位姿仅作合理性校验。
7. 充电点即使是最终航点，也必须先执行 Dock Action，再结束导航任务。
8. 当前 `on_goal_reached()` 需要先按航点类型分发，再判断是否完成整条路线。
9. 对接成功后再根据产品需求发布 `idle`、`charging` 或保持任务状态。
10. 可选使用 `/battery_status.charge_status` 验证实际开始充电。

这一集成阶段不得修改已经通过独立验收的标签检测适配、位姿和控制核心，只增加 Action Client、状态转换和任务结果上报。

## 15. 开发完成定义（Definition of Done）

满足以下条件后，方案 A 才视为完成：

- [ ] 相机 ROS 驱动、标定文件和 TF 已纳入版本管理。
- [ ] AprilTag 标签族、生成文件、ID、实测尺寸和制作规范已归档。
- [ ] 标签板机械图纸、安装位置和维护检查方法已归档。
- [ ] `apriltag_ros` 来源、版本和配置可以复现。
- [ ] 标签 ID 到 `dock_id` 的注册表通过唯一性校验。
- [ ] 标签板到桩端充电面板的外参有书面测量记录。
- [ ] 单标签、多标签和近距离标签切换策略已经验证。
- [ ] Dock Action 支持启动、反馈、取消、成功和明确失败。
- [ ] Dry Run、低速开环、闭环和故障注入测试全部完成。
- [ ] 命令仲裁和控制超时已经实车验证。
- [ ] 对接控制器不依赖地图、Nav2 或充电航点。
- [ ] 对接控制器不依赖 YOLO、训练数据集、ONNX 或 TensorRT 模型。
- [ ] 至少 50 次重复测试达到验收成功率。
- [ ] 成功与失败 rosbag、测试报告和最终参数均已归档。
- [ ] 操作人员具有明确的启动、取消、急停、标签维护和故障恢复说明。
- [ ] 后续 `NavigationManager` 集成接口已经固定，不需要重写核心算法。
